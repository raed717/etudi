// Route group placeholder — the "/" route is owned by /app/page.tsx
// This file exists to satisfy the (marketing) layout group structure
// for future sub-routes like /pricing, /about, etc.
export default function MarketingGroupPage() {
  return null;
}
